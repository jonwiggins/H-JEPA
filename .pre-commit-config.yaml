# Pre-commit hooks for H-JEPA
# Install: pip install pre-commit
# Setup: pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # Black - Python code formatter
  - repo: https://github.com/psf/black
    rev: 24.8.0
    hooks:
      - id: black
        args: [--line-length=100]
        language_version: python3.11

  # isort - Import sorting
  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile=black, --line-length=100]

  # Trailing whitespace and other basic checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=10000]  # 10MB max
      - id: check-merge-conflict
      - id: debug-statements
      - id: mixed-line-ending
        args: [--fix=lf]

  # Pytest - run tests before commit
  - repo: local
    hooks:
      - id: pytest-check
        name: pytest-check
        entry: pytest
        args: [tests/test_phase123_optimizations.py, -v, --tb=short, -x]
        language: system
        pass_filenames: false
        always_run: true

  # Mypy - Static type checking (run manually with: mypy --config-file=mypy.ini src/)
  # Disabled in pre-commit due to environment dependency differences
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       args: [--config-file=mypy.ini]
  #       files: ^src/
  #       exclude: ^(examples|scripts|tests|test_|monitor_|plot_|run_|verify_)/
  #       additional_dependencies:
  #         - numpy
  #         - torch
  #         - types-PyYAML
  #         - types-requests
